# -------------------------------------
# gpcc/src/time/os/epos_arm
# (Sources specific to operating system EPOS on ARM architecture)
# -------------------------------------
target_sources(${PROJECT_NAME}
               PRIVATE
               clock.cpp
              )
